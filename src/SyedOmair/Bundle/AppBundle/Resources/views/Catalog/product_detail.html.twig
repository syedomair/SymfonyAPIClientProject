{#{dump(product)}#}
<div class="row">
    <div class="col-md-4">
        <img src="{{asset('bundles/app/catalog_images/'~product['image'])}}" >
    </div>
    <div class="col-md-8">
        <h4>{{product['name']}}</h4>
        <p>{{product['short_desc']}}</p>
        <ul>
            <li>SKU:{{product['sku']}}</li>
            <li>Price:{{product['price']}}</li>
        </ul>
    </div>
</div>
